require 'rails_helper'

describe "Distribution Center Threshold Page", type: :feature do
  it "has a threshold form" do
    visit "/distribution_centers"
# save_and_open_page
    # expect(page).to have_button('Only return records with more than Trucks Deployed')
    
  end
end